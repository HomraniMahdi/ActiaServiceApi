apiVersion: v1
kind: Service
metadata:
  name: pfe-service
spec:
  selector:
    app: pfe-back
  ports:
    - name: http
      port: 80
      targetPort: 8080
  selector:
    app: pfe-front
  ports:
    - name: http
      port: 80
      targetPort: 80
